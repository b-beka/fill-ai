# FILL AI — План поэтапного редизайна (Redesign Plan)

Концепция: **«Тетрадь · Линейка · Круги»**  
Стек: React + TypeScript + Tailwind CSS + Motion + KaTeX + Canvas Confetti  
Целевая платформа: Web Desktop & Mobile (375px, 768px, 1440px)

---

## Фаза 0 — Разведка и архитектурные контракты (Текущий этап)
- [x] Анализ существующей кодовой базы, типов, API-сервисов и мок-данных.
- [x] Создание документации дизайн-системы: [`docs/DESIGN.md`](./DESIGN.md).
- [x] Создание мастер-плана редизайна: [`docs/REDESIGN_PLAN.md`](./REDESIGN_PLAN.md).
- [x] Создание [`CLAUDE.md`](../CLAUDE.md) с жесткой ссылкой на дизайн-систему.
- [ ] Остановка на подтверждение плана пользователем.

---

## Фаза 1 — Фундамент дизайн-системы (Tokens, Typography, Primitives)
- [ ] Установка шрифтов Fontsource: `@fontsource/unbounded`, `@fontsource/onest`, `@fontsource/caveat`.
- [ ] Настройка дизайн-токенов в `tailwind.config.js` и `src/index.css`:
  - Бумага (`#FBF7EA`, `#F2ECD6`), чернила (`#0E1A4B`), сетка (`#C7D6F2`), синий (`#2340E0`), небо (`#BBD4FF`), солнце (`#FFC72C`), красная ручка (`#E5383B`), штамп (`#1F9D63`), доска (`#0B1440`), мел (`#F4F1E6`).
  - Жесткие тени `4px 4px 0 0 #0E1A4B` и `8px 8px 0 0 #0E1A4B`.
- [ ] Создание библиотеки базовых примитивов (`src/components/ui/`):
  - `Logo.tsx` — двойной круг с линзой F.
  - `Button.tsx` — тактильная кнопка с жесткой тенью и эффектом вдавливания.
  - `Input.tsx` — тетрадная строка с фокусом желтого маркера и ошибкой красной ручкой.
  - `Card.tsx` — тетрадный лист с контуром 2px.
  - `Chip.tsx` — таблетка с чернильным контуром.
  - `Stamp.tsx` — штамп «ПРОВЕРЕНО» / «ДЕМО» под наклоном −6°.
  - `Ruler.tsx` — метрическая линейка с миллиметровыми делениями.
  - `CircleBadge.tsx` — круглый бейдж статистики и аватаров.
  - `NotebookBackground.tsx` — чистый SVG-паттерн клетки с красным полем.
  - `Marginalia.tsx` — рукописная пометка Caveat со стрелочкой.
  - `RedPenUnderline.tsx` — волнистое подчеркивание красной ручкой.
  - `Icons.tsx` — авторские школьные SVG-символы (циркуль, колба, атом, микроскоп, ДНК, транспортир).
- [ ] Создание витрины компонентов `/dev/kit` (`src/components/DevKit.tsx`) в обоих режимах («Тетрадь» и «Доска»).
- [ ] Проверка сборки `npm run build` и типов TypeScript.

---

## Фаза 2 — Оболочка приложения (Header, Shell, PageTransition)
- [ ] Создание `PageTransition.tsx` с двухслойным радиальным расширением круга клика (`clip-path: circle`).
- [ ] Создание `ScrollRuler.tsx` — полосы прокрутки в виде линейки с желтым ползунком.
- [ ] Редизайн `Header.tsx`:
  - Светлая бумага, 2px чернильный контур, ScrollRuler снизу.
  - Логотип `Logo` + надпись FILL AI шрифтом Unbounded.
  - Исправление всех навигационных якорей (`#how-it-works`, `#features`, `#services`, `#testimonials`, `#faq`).
  - Корректный расчет инициалов (пропуск префиксов «Д-р», «Проф.»).
  - Переключатель темы «Тетрадь / Доска» в кабинетах.
- [ ] Интеграция переходов в `App.tsx`.
- [ ] Проверка сборки `npm run build`.

---

## Фаза 3 — Авторизация (Единый Auth-компонент)
- [ ] Создание единого файла `src/data/demoUsers.ts` с профилями преподавателей и студентов вузов Казахстана.
- [ ] Слияние `AuthModal` и `AuthScreen` в универсальный компонент `src/components/Auth.tsx`:
  - Поддержка режимов `variant="page" | "modal"`.
  - Шаг 1: выбор роли через два больших круга («Я преподаватель» / «Я ученик»).
  - Шаг 2: форма на тетрадных строках, 6-значный код в тетрадных клеточках.
  - Валидация с пометками «красной ручкой», предупреждение о Caps Lock, «Запомнить меня».
  - Рабочий шаг восстановления пароля.
  - Демо-вход в виде ученических пропусков с круглым бейджем.
  - Левая половина (для страницы): рукописная живая страница лекции с формулой Гольдмана в KaTeX.
- [ ] Удаление устаревших `AuthModal.tsx` и `AuthScreen.tsx`.
- [ ] Проверка сборки `npm run build`.

---

## Фаза 4 — Лендинг (Модульная структура)
- [ ] Разбиение лендинга на модули в `src/components/landing/`:
  - `Hero.tsx`: тетрадная композиция, рукописные стрелочки, интерактивные круги.
  - `Pipeline.tsx`: интерактивная сцена (Речь → KaTeX-формула → Мини-квиз).
  - `Features.tsx`: разнокалиберный Bento-грид со школьными иллюстрациями.
  - `Audience.tsx`: два больших круга «Преподавателю» / «Ученику» с заливкой.
  - `Pricing.tsx`: тарифы как отрывные листы блокнота с перфорацией.
  - `Testimonials.tsx`: стикеры с рукописными цитатами и пометками «демо-сценарий пилота».
  - `Faq.tsx`: аккордеон на строках тетради с маркером-подчеркиванием.
  - `FinalCta.tsx`: масштабный круг, заливающий экран при скролле.
- [ ] Сборка главного `LandingScreen.tsx` из модулей.
- [ ] Проверка сборки `npm run build`.

---

## Фаза 5 — Кабинет ученика (StudentScreen)
- [ ] Режим «Тетрадь», строгая оптимизация под мобильные устройства (mobile-first).
- [ ] Конспект в реальном времени с эффектом рукописного добавления блоков.
- [ ] Вопрос от учителя в виде карточки с кругами вариантов A/B/C/D (верный — штамп «ПРОВЕРЕНО», неверный — красное перечеркивание).
- [ ] Салют круглыми конфетти (`canvas-confetti` shapes: ['circle']) цветов бренда при правильном ответе.
- [ ] «Что я пропустил?» — липкая желтая записка со шторкой выжимки.
- [ ] Отправка вопроса учителю в виде сворачивающейся «записки».

---

## Фаза 6 — Кабинет преподавателя (TeacherLiveScreen)
- [ ] Режим «Доска» (темно-синий фон `#0B1440`, меловой текст `#F4F1E6`, желтый и небесный акценты).
- [ ] Рамка трансляции с линейкой по контуру, круглый вырез камеры лектора.
- [ ] Индикатор уровня микрофона в виде прыгающих делений линейки.
- [ ] Квиз со столбчатой диаграммой, измеренной в миллиметрах по шкале.
- [ ] Поток заметок с кнопками «Одобрить / Править» и штампом «ПРОВЕРЕНО».
- [ ] Журнал посещаемости группы на тетрадных линиях.
- [ ] Состояния: потеря связи, ошибка микрофона, пустой урок.

---

## Фаза 7 — Менеджер уроков (LessonsManager)
- [ ] Список уроков в виде классного журнала с оценками и статусами.
- [ ] Форма создания урока на тетрадных строках.
- [ ] Зона загрузки слайдов в виде конверта с drag-and-drop.
- [ ] Индикатор связи с FastAPI 8000 в виде кругового маяка.
- [ ] Сообщения об ошибках бэкенда красной учительской ручкой.

---

## Фаза 8 — Контроль качества (Quality Gates)
- [ ] Автоматические тесты на запрещенные конструкции:
  1. `grep` на CSS-градиенты (`linear-gradient`, `radial-gradient`, `bg-gradient-*`) → 0.
  2. `grep` на хардкод hex-цветов в tsx → 0.
  3. `grep` на `lucide-react` и `unsplash` → 0.
  4. `grep` на `font-mono-tag` → 0.
- [ ] Проверка мобильной верстки на 375px: зоны клика ≥ 44px, отсутствие горизонтального скролла.
- [ ] Проверка доступности: фокусные кольца, контраст WCAG AA.
- [ ] Проверка деградации анимаций при `prefers-reduced-motion`.
- [ ] Проверка отображения казахских символов (`Ә, Ғ, Қ, Ң, Ө, Ұ, Ү, Һ, І`) во всех шрифтах.
- [ ] Финальная сборка `npm run build` с 0 ошибок.
- [ ] Формирование итогового отчета по структуре `<final_report_format>`.
